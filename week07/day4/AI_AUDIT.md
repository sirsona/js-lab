## AI Audit

1. Your clickable `<span>` works, but button are more accesible for actions

- **accepted**

2. Inline style could become class-based later, but in large apps, CSS class are
   easier to maintain.

- **accepted**
