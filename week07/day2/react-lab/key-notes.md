# Explain the key prop

- A key is a special string attribute included when creating list of elements
- Keys help React identify which items have changed, are added or are removed
- Keys is given to the elements inside the array to give the elements a stable
  identity
