# AI Audit

1. Poor error handling in `loadDataSafe()`

- incorrect Error constructor usage
- hide real stack trace
  **accepted am implemented**

2. Rendering is too raw (JSON dump)
   **Rejected -> this was to test the api**s
