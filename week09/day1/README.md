# React User List with Fetch API

A React component that fetches and displays user data from the JSONPlaceholder API with proper loading, error, and empty states, plus request cancellation using AbortController.
