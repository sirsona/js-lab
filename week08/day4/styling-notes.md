# Styling Notes

- I chose Tailwind CSS for styling my Task Manager application.

- The main reason is that Tailwind allows me to build UI quickly using utility
  classes without writing separate CSS files.

- It keeps styles consistent and reduces context switching between HTML and CSS
  files.
